# Requirements for Rocket Launch System (GUI + plotting)
# Tested with Python 3.11 on Windows. Tkinter is included in standard Python
# installers on Windows (no separate pip package required).

python>=3.8
numpy>=1.24
pandas>=1.5
matplotlib>=3.6

# Optional / dev tools
pyinstaller>=5.10  # for bundling the GUI into a standalone exe
wheel

# Install with:
#   pip install -r required.txt
